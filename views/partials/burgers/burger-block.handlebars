<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if devoured}}YUMMI YUMMI FOR MY TUMMY!{{else}}LOOKS DELICIUOS!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">BYE BURGER!</button>
</li>
